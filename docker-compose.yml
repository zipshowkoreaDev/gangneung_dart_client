version: "3.8"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_BASE_URL: https://develop.gangneung-dart.zipshowkorea.com
    ports:
      - "3380:3000"
    networks:
      - app
    restart: always

networks:
  app:
    external: true
    name: app
